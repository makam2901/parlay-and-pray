# .dockerignore
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env
.git/
.gitignore
.dockerignore
docker-compose.yml
Dockerfile
*.db # Avoid copying local mlflow db into image
mlruns/ # Avoid copying local runs into image
.metaflow/ # Avoid copying local metaflow cache
*.csv # Usually data is mounted or downloaded, not copied unless small/static

# Add any other files/directories you don't want in the image